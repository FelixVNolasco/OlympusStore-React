export interface Product {
    categories: string[],
    color: ['black'],
    createdAt: string,
    desc: string,
    img: string,
    inStock: boolean,
    price: number,
    size: string[],
    title: string,
    updatedAt: string,
}